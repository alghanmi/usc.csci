# .bash_profile
# You can always get a clean copy of this file from /usc/usc/skel/.bash_profile
# /bin/cp /usr/usc/skel/.bash_profile ~

# For now, all this file does is source ~/.bashrc to make all types of logins
# consistent
if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi
